# Comma-separated list of allowed repo paths (no spaces around commas)
GIT_REPO_PATHS=/path/to/repo1,/path/to/repo2
# Default repo path (must be one of GIT_REPO_PATHS)
DEFAULT_REPO_PATH=/path/to/repo1
BRANCH_NAME=main
SRC_FOLDER=/path/to/serve/src
TOKEN=your_secret_token
PORT=3000
HOST=127.0.0.1
